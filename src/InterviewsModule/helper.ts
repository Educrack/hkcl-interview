export const initialValues = (data: any) => ({
  displayPicture: data.displayPicture,
  name: data.name,
  price: data.price,
  noOfLinks: data.noOfLinks,
  description: data.description,
  course: data.course,
  interviewId: data.interviewId,
  actualPrice:data.actualPrice
});
